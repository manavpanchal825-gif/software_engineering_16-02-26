# What Are The Differences Between HTTP And HTTPS Protocols ?

**HTTP**
>1. HTTP stands for hyper text transfer protocol
>2. HTTP is a communication protocol used to transfer data between a web browser and web server over the internet
>3. HTTP is a not secure because it sends data in plain text,formate can anyone reads a data normaly
>4. HTTP does not use any encryption method to protect a data
>5. in HTTP sensetive data are easily intercepted
>6. website using HTTP start with: http://
>7. HTTP uses port 80 by default for communication
>8. HTTP does not require any digital certificate to verify the website
>9. HTTP website are considered less secure and may show a not secure warning browser.. 

**HTTPS**
>1. HTTPS stands for hyper text transfer protocol secure
>2. HTTPS is a secure because it invrypts the data before sending
>3. HTTPS is the secure version of http that protects data using encryption
>4. HTTPS uses SSL(secure stacket layer) And TSL(transport layer security)
>5. in HTTPS the data is encrypted so even if someone interceptd it they cannot read data.
>6. website using HTTP start with: https://
>7. HTTPS uses port 443 by default for secure
>8. HTTPS requires SSL/TSL certificates issude by a trustes authorites
>9. HTPPS website are trusted more by users and are prefered by search engines for better ranking